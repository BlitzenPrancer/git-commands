# git-commands
git commands

1) git init                       # initialises the directory
2) git add main.go                # adds the file to the staging area
3) git commit -m 'some message'   # commits the change to local version control system
4) git log                        # shows log
5) git status                     # shows status of your current commit
6) git push origin master         # pushes code to github
